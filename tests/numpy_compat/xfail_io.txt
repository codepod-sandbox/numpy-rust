# Known failures for npy/npz I/O compat tests
# Format: ClassName.test_method_name (one per line)
# Blank lines and lines starting with # are ignored.

# Requires 2GB+ memory -- hard-skipped in runner (would hang)
# TestSavezLoad.test_big_arrays (hard-skipped via _SKIP_TESTS)

# Resource management / 1024-iteration fid test -- hard-skipped in runner
# TestSavezLoad.test_closing_fid (hard-skipped via _SKIP_TESTS)
